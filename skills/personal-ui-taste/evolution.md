# 演化协议与反馈记录

本 skill 是活的个人偏好库。用户已授权持续维护：在实际前端合作中，遇到明确的审美反馈与认可，应更新本目录，而不是仅记在某个项目会话里。

## 更新规则

1. **先读再改**：读取实际加载路径下的最新 `SKILL.md`、本文件和相关模式，保留其他场景新增内容；不要根据旧版本覆盖整份文件。
2. **区分证据**：
   - 用户明确说喜欢/不喜欢/以后都如此：记为 **Confirmed**。明确拒绝本身就是证据，不必等待替代品被接受。
   - 用户认可已展示方案：认可该范围的设计，不推断其他颜色模式/设备/业务也被认可。
   - 只有 agent 自己觉得好、截图好看、编译通过或用户未反馈：至多 **Candidate**，不能进入默认规则。
   - 特定品牌/当前项目例外：记为 **Scoped**，不推翻全局偏好。
3. **提炼而非转录**：记录“偏好 + 场景 + 为什么 + 接受/拒绝证据 + 解决方式”，不要复制聊天日志或积累任意 CSS 值。
4. **决定放哪里**：跨场景稳定品味 → `SKILL.md`；已有场景的组件模式 → `patterns.md`；新的独立场景 → 按需建立 `scenarios/<scene-name>.md` 并更新场景索引；时间/证据/状态/变更 → 本文件。相同事实只维护一处正文，其他地方引用 ID。
5. **处理冲突**：最新明确反馈在其适用范围内优先。局部例外保持局部；无法判断是否改全局时，继续当前任务并只问一个针对性问题，不阻塞不相关工作。
6. **允许增删改查**：按下表操作，保持主文件只呈现有效偏好。更新是维护知识，不是无限追加日志；过时、重复和被明确否定的条目可删改。
7. **修订版本**：一批确认的行为/品味更新递增 minor，文字澄清递增 patch；大范围推翻体系再升 major；同步 frontmatter 的 `updated`。不要为每句措辞都建版本。
8. **校验并告知**：检查 frontmatter、相对链接、规则不冲突、引用文件存在。简短告知“已把 X 记入全局品味”，不要声称未写入的文件已更新。
9. **路径不可写时**：给出精确的待应用更新和目标位置，说明尚未持久化；不要悄悄只写项目副本却称为全局更新。

## 按场景增删改查

| 操作 | 做法 |
| --- | --- |
| 查询 | 先按场景索引定位，再查规则 ID、组件、关键词与适用范围；回答当前规则，必要时再查看反馈记录 |
| 新增 | 已有场景补充细节，新增相应规则；新场景经过调试确认后添加场景 ID 和索引。不要整份复制旧场景，也不要提前造空文件 |
| 修改 | 定位原规则就地改写，保留适用的其余内容；同一规则保持稳定 ID，新范围分支单独标注 |
| 删除 | 用户要求删除、规则重复或已失效时，从有效规则和对应索引/引用中清除；删除场景文件前检查引用。记录一句删除原因即可，不强制保存被要求移除的原文 |
| 合并/拆分 | 重复规则合并并更新引用；一个场景出现不同主题/设备下的稳定差异时拆分范围，不互相覆盖 |

规则 ID 使用 T01…，场景 ID 使用 S01…；已有 ID 不复用给无关内容。新增场景先继承仍适用的通用偏好，再记录自己的差异。每轮更新只改本次反馈覆盖的范围。

## 内容与迁移

- 只保存 UI 设计经验。案例、反馈与示例使用通用组件名称，不保留项目叙事、专有名词或功能实现背景；这条要求同样适用于变更记录。
- 新截图使用通用演示内容并随目录保存，用相对路径引用；不依赖临时文件。
- 全部规则是可读 Markdown。其他服务器复制**整个目录**；此机制不会自动联网同步。
- 两台机器都更新了：逐项按稳定 ID、反馈日期和范围合并，保留双方记录；版本号大不等于审美证据新，不盲目覆盖。
- 外部笔记可以指向此 skill，但当前规则应以本目录为准。新场景成熟后再补模式，不提前造空模板目录。

## 反馈记录模板

```markdown
### YYYY-MM-DD · 简短主题 · vX.Y.Z
- 状态：Confirmed / Candidate / Scoped / Superseded / Removed
- 场景与操作：Sxx；新增 / 查询结果 / 修改 / 删除 / 合并 / 拆分（仅实际变更需留记录）
- 范围：页面或组件、设备/主题等
- 证据：用户原话短摘或明确标注的转述；方案有无实际展示/认可
- 规则：Txx；若替代旧项，列出继任规则；删除时记录原因，不重复被移除内容
- 变化与原因：旧行为 → 新行为；解决哪个体验问题
- 参考：相关模式或场景文件，或随 skill 携带的通用演示素材
```

## 已确认记录

### 2026-09-13 · 跨页面个人品味基线 · v1.0.0

- 状态：Confirmed。
- 范围：浅色信息密集界面、表单、有序列表、数据图表和状态历史；经过多轮实际页面修改与反馈。
- 最终认可原话：“这部分UI设计在我看来已经比较符合我的心意”；用户要求保存为全局 skill，供其他项目/服务器使用并持续演化。
- T01–T04：拒绝“像 markdown 渲染的代码块”的控件；参考 NewAPI/Firecrawl/Mixedbread；认可比例字体、柔和灰底、圆角、轻量状态标签与全站一致性。
- T05：要求点击一个柱状块后高亮它、其他变淡，tooltip 内对应条目同样突出；此前已有轻微悬浮放大请求。
- T06：最右侧是最新数据、最常先看，明确拒绝提示名称因贴边空间不够被挤压折行；接受自然宽度 + 向左翻转。
- T07：图表与顶部筛选距离远导致往返滚动，偏好图旁提供与顶部同步的维度筛选。
- T08：认可可拖拽的紧凑有序列表；偏好细矩形红黄绿灯带并能看长历史。
- 图表细节：长堆叠块以浅色高亮，比例趋势平滑；空时段展示选择需明确说明，见 patterns.md。
- 验证证据：桌面/手机实际浏览器操作与截图检查，包括右边长条目、聚焦切换、键盘、搜索、拖拽；最终审美认可来自用户，而非测试结果。

### 2026-09-13 · 纯设计口径与场景维护 · v1.1.0

- 状态：Confirmed；适用于整份 skill 的维护方式。
- 用户要求：只关注 UI 设计；新场景经过反复调试后增量保存，细节支持增删改查。
- 变化：案例统一为通用组件语言；增加 S01 场景索引与按需扩展方式，明确允许修改、删除和合并旧规则。

### 2026-09-13 · 冷灰蓝卡片工作台与整点选择 · v1.2.0

- 状态：Confirmed；S02 为按场景认可，配对深色数值仅为源码参考，未推定所有深色细节获认可。
- 场景与操作：新增 S02；补充 T09/T10；收窄 T01/T08 的密度约束以允许预览卡片与密集列表/灯带共存。
- 证据：用户明确喜欢“主题风格，按钮风格，字体风格，大矩形框的样式”；认为状态不密集且有跳转时现有形式“可以保留共存”；时间偏好“24 小时”“分钟默认定位到 00”。用户另明确要求优先看前端源码。
- 变化与原因：提炼实际主题 tokens、系统 UI 字体、细边框圆角面板、图片主导卡片与渐进操作；状态密度由内容和交互决定，不因名称相同强套细灯带。
- 时间交互：新增分钟默认整点、允许手动改分钟，明确避免空原生 time 的浏览器显示差异与默认时间意外提交；具体新实现尚待用户进一步反馈，不把技术验证等同于审美认可。
- 参考：[S02](scenarios/preview-workbench.md)、[时间选择模式](patterns.md#7-每日时间点24-小时制与整点默认)。

## 候选与局部例外

### 2026-09-15 · 数据看板的塑料感 · v1.3.0

- 状态：Confirmed（拒绝现有呈现）；新的替代设计尚未获用户认可。
- 场景与操作：S01 研究指标/曲线；新增 T11。
- 证据：用户明确反馈页面“非常的塑料感”，且“图表非常难看”，质疑是否真正参考个人品味。
- 变化与原因：默认色值与圆角不是验收标准；数据层级、曲线区分、标签可读性需要实际视觉审查。不得以浏览器功能检查通过代替审美认可。
- 范围：信息密集的研究看板；不推翻 S02 已认可的预览卡片风格。

### 2026-09-15 · 筛选布局认可与图表亮色 · v1.4.0

- 状态：Confirmed；S01 研究看板，更新 T11。
- 证据：用户明确喜欢“左边筛选栏”和“上面的服务器切换栏”；仍偏好之前的柱状图与折线，拒绝新配色“太暗了，太沉了”，要求“亮色一点”。
- 变化：保留侧栏和平台切换，图表回到明亮蓝紫青绿与常规柱宽。移除“细柱、暗色”候选，不把整体塑料感归因于亮色图表。
- 范围：布局已认可，具体新色值仍需实际呈现后反馈；不把认可扩大到所有控件或其它场景。

### 2026-09-15 · 折线提示焦点与交付验收 · v1.4.1

- 状态：Confirmed（用户明确要求联动）；对已有 T05 的适用范围与验收方式作澄清。
- 证据：用户指出悬浮/选中曲线或数据点时，信息框未突出对应条目，要求其他条目变淡，并提醒这是既有偏好。
- 变化与原因：旧入口突出堆叠色块，容易漏掉折线；改为多系列图表规则，图表任务必读 §1，并要求圆点和无圆点线段的真实命中验收。原规则已明确“图与提示同步”，此次遗漏也属于执行与验证不足，不能全归因于文档。
- 参考：patterns.md §1；记录线段事件可能缺少稳定 ID、库内 emphasis 不会自动更新自定义 tooltip 的实现教训。未将技术检查通过记为用户已认可全部视觉细节。

### 2026-09-15 · 密集图表问题的症状索引 · v1.5.0

- 状态：Confirmed（问题与按症状检索的维护要求）。
- 证据：用户报告多条折线的信息框遮挡目标、条目看不全且不能滚动，提出每张图各占一行；随后要求将实际问题和解法存入 skill，用户一旦提出就能立刻索引，不要求事先自主发现全部问题。
- 场景与操作：S01；新增入口症状索引和 patterns.md §2.1，关联 T05/T06。
- 变化与原因：从只有视觉偏好与局部定位规则，补齐“症状 → 原因 → 处理选择 → 实际操作验收”；明确扩大画布、固定详情、可滚动区域各解决什么问题，避免盲目套用同一种布局。

### 2026-09-15 · 就近悬浮优先 · v1.6.0

- 状态：Confirmed；S01 多系列图表，修正 T06 与 §2.1。
- 证据：用户明确不喜欢将信息移到图下，认为视觉体验下降；认可扩大画布，要求提示框回到高亮目标附近自动定位，必要时支持滚动。
- 变化：删除优先固定详情的建议，改为扩大画布、目标附近自动避让、可进入滚动的悬浮框；只有明确要求或认可时才采用固定详情。技术上解决遮挡，不代表信息位置的改变获认可。
