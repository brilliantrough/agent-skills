diff --git a/src/adapters/pi/extension.ts b/src/adapters/pi/extension.ts
index bd5d4d9..8f9b3fb 100644
--- a/src/adapters/pi/extension.ts
+++ b/src/adapters/pi/extension.ts
@@ -655,11 +655,15 @@ export default function piExtension(pi: any): void {
       // already tell the model what each tool does. This anchor gives the
       // deliberate choice (which tool for which scenario) without the full
       // block/redirect/memory/tool-selection hierarchy.
+      // Fork 口径（2026-09-19）：本机是 ~/Linewrite/forks/context-mode（纯上游 clone + 两层补丁）。
+      // 锚点原本还写着 "Upgrade → ctx_upgrade"，而升级会把上游装回来、扶掉改名补丁，
+      // 导致与 pi-magic-context 的 ctx_search 重新撞名——所以这里不再提升级；
+      // 并且 "Read/edit files →" 改掉：ctx_execute_file 只能读（它把文件读进 FILE_CONTENT），不能编辑。
       parts.push(
         "context-mode active. Hierarchy: ctx_batch_execute > ctx_execute > ctx_execute_file > ctx_search. " +
-        "Read/edit files → ctx_execute_file. Multi-command research → ctx_batch_execute. " +
+        "Read files / bulk processing → ctx_execute_file. Multi-command research → ctx_batch_execute. " +
         "Web pages → ctx_fetch_and_index then ctx_search. Index docs → ctx_index. " +
-        "Stats → ctx_stats. Doctor → ctx_doctor. Upgrade → ctx_upgrade. Purge → ctx_purge."
+        "Stats → ctx_stats. Doctor → ctx_doctor. Purge → ctx_purge (destructive, needs confirm:true)."
       );
 
       // Pi-3 + Pi-4: Always build active_memory (not just post-compact),
diff --git a/src/server.ts b/src/server.ts
index 489116a..adc058c 100644
--- a/src/server.ts
+++ b/src/server.ts
@@ -803,7 +803,10 @@ function getUpgradeHint(): string {
   const name = _detectedAdapter?.name;
   if (name === "Claude Code") return "/ctx-upgrade";
   if (name === "OpenClaw") return "npm run install:openclaw";
-  if (name === "Pi") return "npm run build";
+  // 本机是 fork（纯上游 clone + 两层补丁，见 DEC-CTXMODE-FORK-001）。上游的 "npm run build"
+  // 只重建产物、不会重打改名补丁，照它做会得到一个 ctx_* 与 ctxm_* 混杂的构建（ctx_search
+  // 立即和 magic-context 撞名），所以这里指回我们的同步脚本。
+  if (name === "Pi") return "bash ~/Linewrite/skills/agent-skills/context-mode/setup.sh";
   return "npm update -g context-mode";
 }
 
@@ -4322,10 +4325,12 @@ server.registerTool(
       openWorldHint: false,
     },
     description:
-      "Upgrade context-mode to the latest version. Returns a shell command to execute. " +
-      "You MUST run the returned command using your shell tool (Bash, shell_execute, " +
-      "run_in_terminal, etc.) and display the output as a checklist. " +
-      "Tell the user to restart their session after upgrade.",
+      "FORK: do not call（本地 fork，勿调用）. This install is a fork of upstream whose tool names " +
+      "are renamed to ctxm_* after the build so they cannot collide with pi-magic-context's " +
+      "ctx_search; the upstream upgrade would reinstall upstream and wipe that rename. " +
+      "The upgrade path on this machine is `bash ~/Linewrite/skills/agent-skills/context-mode/setup.sh`. " +
+      "Legacy behavior of this tool: returns a shell command that upgrades context-mode to the " +
+      "latest version.",
     inputSchema: z.object({}),
   },
   async () => {
